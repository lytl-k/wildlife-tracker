<% provide(:title, "Regions") %>
<div class="container">
  <h1>Regions</h1>

  <% flash[:notice]&.each do |msg| %>
    <div class="alert alert-danger">
      <%= msg %>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <table class="table table-bordered">
        <thead>
          <tr>
          <th>Name</th>
          <th></th>
        </tr>
        </thead>
        <% @regions.each do |region| %>
          <tbody>
            <tr>
              <td><%= region.name %></td>
              <td>
                <%= link_to "Edit", "/regions/#{region.id}/edit", class: "btn btn-primary" %>
                <%= link_to "Delete", "/regions/#{region.id}", class: "btn btn-primary", method: :delete %>
              </td>
            </tr>
          </tbody>
        <% end %>
      </table>
      <div>
        <br>
        <%= link_to "New", '/regions/new', class: "col-md-3 btn btn-primary" %>
        <div class="col-md-6"></div>
        <%= link_to "Back", '/home', class: "col-md-3 btn btn-primary" %>
      </div>
    </div>
  </div>
</div>